name: matrix

on: push

jobs:
  job:
    name: this-is-the-job / ${{ matrix.key }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        key:
          - foo
          - bar
          - baz
    steps:
      - run: echo '${{ matrix.key }}'
